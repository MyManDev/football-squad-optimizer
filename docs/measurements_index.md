# Measurements Index

One line per committed real-data measurement artifact: what it is, what it found, and
where it came from. Every artifact here is measurement-only in the sense that matters —
**nothing in this index promoted a model or changed the operational control.**

One artifact **did** read the locked 2025-26 holdout: `fw10_holdout`, once, under the
pre-registered protocol in `fw10_holdout_plan.md`, with three owner approvals recorded
first. It did not promote its challenger. That is stated here rather than left to the row,
because the blanket claim this paragraph used to make — that nothing here had read the
holdout — stopped being true the moment the run was recorded, and a header a reader trusts
must not contradict its own table.

Regenerated artifacts must keep passing `scripts.run_measurement_preflight`.
`tests/unit/test_measurements_index.py` holds the rule that every committed artifact appears
here (ADR 0003, rule 1).

## Deterministic policy

| Artifact | Finding | PR |
| --- | --- | --- |
| `baseline_bayesopt` | First real BO run: best observed `fw10-bw0` at 56.50 mean realized points (147 folds) | #57 |
| `baseline_policy_grid` | Exhaustive 56-cell ground truth; fw10 column holds ranks 1–7; **BO regret 0.0000**, optimum found at iteration 3 | #61 |
| `fw10_screening` + `fw10_frozen_candidate` | Official screening DoE froze `fw10-bw0` as the eligible challenger; holdout plan in `fw10_holdout_plan.md` (run deferred) | #65 |
| `fw10_season_robustness` | Challenger-vs-control delta per development season, in isolation | #74 |
| `fw10_holdout` + `fw10_holdout_note` | **The locked holdout, spent.** `fw10-bw0` **not promoted**; `fw05-bw0p1` remains the operational control. Paired mean **+1.405** on 37 folds of 2025-26, 90% interval **[−2.622, 4.000]** — the effect-size gate passed (≥0.5) and the confidence gate failed (lower bound below zero). Development was +2.728 [0.762, 4.544] over 147 folds, so the challenger did not reverse; one season's interval is simply wide. `development_seasons_accessed: false`. Per the pre-registered plan the fw10 hypothesis is closed: no re-runs, no threshold changes. Limitations, including the holdout's prior exposure and a pre-run coverage read, are in the note | #168 |
| `issue43_candidate_declaration` | The #43 candidate frozen before its run: `learned_rate_calendar_candidate_v1`, one changed component (`expected_points_rate`), 14 components named frozen. Declaration fingerprint `f72962a1…`, benchmark configuration `b64a3ab9…`, both computed from the typed objects the run would use. `formal_run_completed: false` — this freezes a run rather than reporting one | #43 |
| `learned_benchmark_development` | Learned rate against the deterministic baseline over 147 development folds: **53.78 → 56.89 mean realized points, +3.12**, winning 80 folds, losing 57, tied 10, and changing the captain on 97. Explicitly **not** a promotion — `automatic_promotion: false`, and the record says a separate gate must be defined first. The Ridge arm is an integration reference, not a rival model; opening gameweeks and the holdout are outside it | — |
| `production_gate_judgement` | The development gate the two-stage production candidate **did not clear**: verdict `no_promotion_control_retained`. Three of four gates passed — against the baseline +4.4898 [2.5643, 6.6939] and a positive mean against the ridge reference (+0.6463) — but the **ridge lower bound of −1.6194 failed**, so the candidate is not distinguishable from the reference. Means over 147 feasible folds: baseline 53.26, production 57.75, ridge 57.10. This is why the live path still projects with the operational control | — |
| `selection_optimism` | Winner's curse located: roster residuals unbiased (−0.004), selected starters −2.96, captains −3.86, top-5 ranked −3.53 | #73 |
| `shrinkage_grid` | Does decision-side position-mean shrinkage improve realized squads? (`position_mean_shrinkage_v1`) | #74+ |

## Scenario and risk

| Artifact | Finding | PR |
| --- | --- | --- |
| `scenario_bayesopt` | First 3-factor search; best `fw6-bw0-ra0` at 57.11; risk aversion's mean cost measured | #60 |
| `scenario_bayesopt_deterministic` | Same recommendation under a deterministic work budget (`det=2.0`); trace machine-independent | #64 |
| `risk_frontier` | **Every ra>0 worsens mean AND floor** (q10 −10..−28); premium buys negative protection | #67 |
| `scenario_calibration_audit` | Cause found: +34.5 decision-level location bias; realized scores in the scenarios' extreme lower tail (PIT 0.07) | #68 |
| `scenario_calibration_audit_{development,online,dev_dgw,online_dgw}` (+correction note) | Decision-level **selection-optimism shift** (−36.3 at squad level from `selection_optimism_profile_v1`; or online from earlier folds) fixes the location: mean PIT 0.07 → 0.55/0.51, bias +34.5 → −1.8/+2.7; squad-level spread slightly narrow both ways (PIT tails 0.14/0.14 vs 0.10) — see `_disp` row; double-gameweek scale 1.45 and rival comparison (same scenarios, shared players cancel) added; live risk applies the shift by default and states its limits | — |
| `scenario_calibration_audit_{development,online}_disp` (note section) | Squad-level dispersion: online RMS of location-corrected standardized gaps settles at **1.15** (spread ~15% narrow); widening by it moves every tail rate toward nominal or leaves it (development shift: PIT>0.90 0.14→0.08, realized<q10 0.16→0.14; online shift: PIT<0.10 0.22→0.16, q10 0.24→0.19) but at 37 folds the intervals include 0.10 before and after — **live default stays 1.0**, `--risk-dispersion-scale` exposes the candidate and the report states the raw-spread limit | — |
| `rank_objective_rehearsal` (+note) | First rehearsal of the rank-probability objective (`rank_probability_objective_v1`) against the fold's own template squad, 37 folds x 100 scenarios: **honest negative** — claimed P(ahead) ~0.5 at every budget vs realized 0.33-0.41 [90% intervals topping out at 0.48-0.54], 0.51 vs 0.22 among proven-optimal folds; realized cost above expected cost everywhere; 7/33 ties at budget 0; only the tightest budget mostly proven (0.82 -> 0.00 unconstrained), so the menu is not monotone. Mechanism verified, claim not yet honest. **Second pass** (`_heldout`, contract v2: claim read on 100 held-out scenarios, ties stated, three provable phases + rival hint): winner's curse measured at ~0.10 (in-sample 0.51–0.55 vs held-out 0.40–0.45) and removed from the claim; claim now inside the realized interval at 3 of 4 budgets (realized strict-ahead 0.30–0.35, level 3–16%); menu monotone again; realized cost still ~+3 vs expected ~+1 — the cost side needs the same correction. Still a measurement instrument, not on the live path | — |
| `scenario_calibration_audit_located_k10/_k2` | Honest negative result: per-player location component does not fix the bias — the curse is selection-time, not player-persistent | #70 |

## Uncertainty and recalibration

| Artifact | Finding | PR |
| --- | --- | --- |
| `control_uncertainty_calibration` | Player-adaptive holds 0.90 coverage at ~11.5% narrower intervals; fully development-internal | #62 |
| `control_residual_export` (record) | Control-regime `oos_residual_export_v1`: exactly 147 folds / 101,447 rows; preflight-clean | #59 |
| `candidate_residual_export` (record) | Calendar-aware `learned-rate-v2` half of the pair; control regenerated at the same commit and byte-identical across three commits | #80 |
| `time_aware_recalibration` | Held-out coverage **unchanged** on single gameweeks (0.9019 both) at **7.2% narrower** intervals; both regimes **undercover doubles** (0.83 / 0.80 vs nominal 0.90) | #82 |
| `issue43_handoff_acceptance` + `issue43_stage_a_review` (records) | Handoff accepted item by item with independent re-runs at `93a87d6`: control export hash reproduces across machines, **candidate export does not** (BLAS-level last-bit differences; content identical to 4 dp); declaration fingerprints reproduce byte for byte; optimization-side Stage A review done, reading (a) confirmed, v2 clean enough to freeze; freeze pending the third owner | — |
| `issue38_calibration_decision` (record) | Operational calibration stays bound to the control's residuals; #38 closes with the #43 verdict either way; the shared double-gameweek undercoverage (0.83/0.80 vs 0.90) is a calibration fact — a fixture-group conformal axis is this layer's next measured step | — |
| `fixture_group_conformal` (+note) | The #38 follow-up on the control's export, chronological split (88 calibrate / 59 held-out folds): a position-by-fixture-group axis lifts **double-gameweek coverage 0.849 → 0.901** (nominal 0.90; width 7.2 → 10.1 on 1,265 double rows) while singles narrow (7.25 → 6.90) and stay above nominal; overall coverage unchanged, mean width down; no cell needed the fallback — the evidence a `projection_uncertainty_v2` declaration rests on | — |
| `control_uncertainty_calibration_v2` + `projection_uncertainty_v2_declaration` (record) | The declaration the #38 decision asked for: `UncertaintyConfig(grouping="position_fixture_group")` is `projection_uncertainty_v2` (position × single/double_plus, blanks zero, calendar required, v1 untouched and its fingerprint reproduced); on the control, 2024-25 held out: overall 0.910/6.89 (v1 0.910/6.85), doubles 0.863 at width 9.83 (364 rows), every cell self-fitted; **operational default since 2026-08-18**: the risk-screening and control-calibration runners default to v2 with the archive calendar attached (`calendar_from_archive`), the live risk layer widens doubles from the capture's calendar (`--risk-double-gameweek-scale` 1.45), `UncertaintyConfig()` itself stays v1 so recorded artifacts reproduce; GW1 replay byte-identical | — |
| `recalibration_dry_run` (+note) | Preflight-gated time-aware pipeline proven on real fw05-vs-fw10 regimes; fw10 tighter at unchanged coverage | #63 |

## Planning

| Artifact | Finding | PR |
| --- | --- | --- |
| `multi_gw_rehearsal` | Planner vs myopic on real windows: +1.17 net points/window under naive projections | #66 |
| `planner_doe` | Horizon length is the live control (H2 +4.83 / H4 −3.67); hit cost matters only below 4; discount dead | #71 |
| `projection_horizon_run` (record) | First real multi-gameweek handoff; an opening capture yields a **flat** horizon because the published calendar has no blanks or doubles | #83 |
| `horizon_decay` | Projection MAE grows only **+7.8% over three gameweeks** (~2.6%/GW); doubles decay faster (+10.6%) than singles (+7.5%) — so the H4 planner loss is mostly **not** projection staleness | #85 |
| `planner_horizon_seasons` (+note) | Four seasons / 23 windows per horizon: H2's +4.83 was one season (4-season mean −0.57); no horizon beats myopic beyond noise (SE 1.7–3.4); H4 −4.87 loses on **selection, not hits** (planner pays fewer hits than myopic), concentrated in windows with **no** calendar structure → information staleness at the top of the ranking, not calendar handling; wall-clock-capped, recommendation-quality | — |
| `export_precision` | Unrounded, **all 58,855 non-zero rows move** under a 1e-15 perturbation — why two owners hashed the same export differently; **9 dp moves none**, so the export now writes nine decimals | #94 |
| `candidate_runtime` | Checklist item 15: learned candidate **590 s** over 147 folds (4.02 s/fold) against the control's **11.7 s** — about fifty times the cost, projection-only, no solver | — |
| `planner_horizon_rolling` (+note) | 66 windows/horizon, four seasons, deterministic budget: **re-planning weekly removes the selection loss (H4 −7.94 → +0.35) but replaces it with a hit bill** (rolling H4 pays 14.5 hit pts/window vs 5.8 myopic; advantage −8.32 [−11.1, −6.3]); rolling H3 −2.30 [−4.2, −0.4]; one-shot H4 −5.15 [−7.0, −0.9]; no horizon beats the weekly baseline in either mode — churn is structural under naive calendar scaling; next: transfer discipline / discount in rolling mode, then chips | — |
| `season_chain` (+note) | Four seasons walked as one chain each (37 decisions, state carried, game sell rule, chips once per window): **chips are worth ~70–90 net/season and mostly the wildcard** (WC +60–65 hits avoided, BB +11–17, TC +3–11; chips-on vs off +69 [+0.56, +2.99]/week, reserve rule +88); a finite horizon **burns BB/TC in GW2–4** — the double-gameweek reservation rule adds +18 (myopic) / +66 (rolling); **rolling H3 loses −156/season without chips, −110 with, −44 with reservation** [−2.93, +0.16]/week — churn does not become chips (265 vs 142 hit pts); two planner fixes (deferred equal-value chips, pinned wildcard FT accounting) | — |
| `season_chain_value` | Chip **holding value** (BB 20 / TC 18 / WC 12 pts, planner-side option value) vs the calendar rule at lookahead 1: **+97 vs +88 per season** over no chips [+1.24, +3.86]/week; TC timed onto the biggest double (20 pts realized in 3/4 seasons), WC held to larger rebuilds (+76 vs 63), BB still burned in GW2 by inflated opening projections — combine holding values with the calendar rule for BB next | — |
| `transfer_discipline` (+note, +`_rolling`) | Planner hit cost {4,6,8} × transfer cap {none,2,1} × banked-FT value {0,1,2} on the season chain: at **lookahead 1 no discipline is robustly better** than the rule (main effects −13…−28; seasons flip sign; banked value alone is significantly negative); at **lookahead 3 a one-transfer cap adds +140/season** in every season [+2.18, +5.78]/week — the capped rolling planner (2016) is level with the weekly control (2008; +0.22/wk [−1.14, +1.39]) and +33 over the capped weekly control: the first positive evidence for a horizon, conditional on the cap | — |
| `season_chain_hybrid`, `season_chain_blind` (+`transfer_discipline_blind_rolling`) | Hybrid chip policy (BB reserved for doubles, TC/WC held) **+102/season** over no chips [+1.50, +4.15] vs reserve +88 / value +97 — within ~15 of each other; **calendar-blind control** (no fixture-count scaling): weekly control loses **58 net/season**, chip value halves (reserve +48, value +52), rolling cap-1 still +122 → the live GW2+ handoff must be calendar-aware | — |
| `season_chain_freehit`, `transfer_discipline_value_rolling` | Free hit (planner contract v2) on the chain: played at the seasons' big blank/double weeks (GW25/32) for **+9 (reserve) / +17 (hybrid) per season**, once negative; all four chips **+103 / +114** over no chips. Rolling cap-1 with holding-value chips **1968** — below capped rolling under the calendar rule (2016) and the weekly control (2018): the capped rolling planner draws with the weekly control, it does not lead | — |
| `chip_bayesopt` (+note) | Bayesian search (`deterministic_policy_bo_v1`, 20 evaluations, season-robust objective mean − 1 sd) over chip holding values and the planning hit cost on the lookahead-1 hybrid season chain: candidate `3xc=20, wildcard=24, bboost=0, freehit=0, hit cost 7` — nets 2097/2122/1952/2044 (mean **2053.8**) vs the recorded hybrid reference 2035.0, **ahead in all four seasons** (+12/+33/+19/+11); most of the robust gain is variance reduction from hit cost 7-8 (spread ~60 vs ~130 at cost 4), `wildcard_hold=24` sits on the grid edge, `bboost_hold` inert under hybrid; a candidate for the next season chain, no promotion | — |
| `season_chain_tuned` | The chip-BO candidate as the chain's `tuned` mode vs `hybrid` and `off` on four seasons (L1): reproduces the search's nets exactly (2097/2122/1952/2044); tuned − hybrid **+0.51/week, 90% block-bootstrap [−1.14, +2.13]**, positive-week share 0.43, +18.8/season from fewer hits (60 vs 172 hit points) — the four season signs agree, the weekly evidence does not leave zero; against off, tuned +3.62 [+1.88, +5.57] and hybrid +3.11 [+1.82, +4.69]: the chips are the effect, the tuning is not | — |
| `chip_bayesopt_wide`, `chip_bayesopt_value` (note follow-up) | Wider `wildcard_hold` grid (0..40) under hybrid moves the robust optimum to `3xc=5, wildcard=28, freehit=20, hit cost 8` (mean 2072.5, spread 98, robust 1974.6 — higher mean, lower robust than the first run; wildcard 40 lost three times); value mode: `3xc=10, wildcard=32, freehit=20, hit cost 7` mean 2064.8, spread **46.6**, robust **2018.2** (best of the three searches, ahead of the hybrid reference in all four seasons, +30 mean). Only the hit cost (7–8), freehit_hold at the grid top and wildcard 28–32 recur; the surface is flat within the season spread — no promotion | — |
| `opening_newcomer_study` (+note) | A third of an opening pool has no prior record (199/587 live; 33-39% historically) and two thirds of them never play. The shipped price prior over-predicts them **2x** (1.387 predicted vs 0.686 realized, bias +0.70); refitting on newcomers alone gains **+0.333** MAE [+0.307, +0.355], ownership **+0.363**, the game's own xP **+0.401** — but **ordering worsens in every season** (within-position Spearman 0.51/0.47/0.51 for price vs 0.36/0.32/0.35 for the richest candidate), so ownership and xP fix the level, not the order. Decision level: +12/+0/+7 realized points, never worse, driven by crowd-known signings the price line cannot see (Haaland 2022-23, 56% owned, 13 points). Movers over-predicted (bias -0.51 vs -0.33 for stayers) but the sign flips by season on 115 rows. **Gate fails on ordering: nothing promoted**; the next study is the two-part P(plays) x points-if-plays shape | — |
| `schedule_signal_study` (+note) | Stage one of the recommender programme, 4,403 paired five-week windows: the **calendar passes** (+0.114 MAE [+0.078, +0.151], sign consistent, +14.7 realized points per window) but **difficulty over the calendar fails** — the published rating improves error +0.026 [+0.007, +0.044] yet reverses sign in 2022-23 and loses **−2.6 points per window** and **−8.5 net per transfer** at the decision level; a club-strength proxy does not even clear accuracy (−0.006 [−0.028, +0.014]). Most of the difficulty is already priced into recent form; S1/S2 must beat the published rating at predicting goals and be judged at the decision level, not on error | — |
| `team_rating_study` (+note) | Stage two: Dixon-Coles attack/defence fitted to goals, refitted at 98 judged gameweeks, half life and ridge selected per season on earlier seasons. **Goals pass** (+0.1020 log-likelihood per fixture [+0.0756, +0.1323], every season); **players pass** — the rating orders attackers' points **three times** as well as the published difficulty rating (+0.0631 vs +0.0213) and better on defenders (+0.0674 vs +0.0473); **clean sheets fail on consistency** (pooled Brier +0.0035 [+0.0006, +0.0062] but better in only 1 of 3 seasons, the two losses being ties of −0.0006 and −0.0003). Calibration is good through the middle and breaks above 0.5 (predicted 0.534, realized 0.325 over 40 fixtures). **Gate fails; nothing promoted** | — |
| `team_rating_cs_remeasure` (+prereg) | S1's one failed clause, in the measurable form the Decision 1 ruling forced: same Brier comparison, same threshold, the contaminated published-difficulty baseline replaced by a walk-forward logistic on the opponent's **previous-season table** (knowable at every deadline; promoted clubs at the measured promoted mean). **The clause passes, 3/3 seasons** (+0.0022/+0.0123/+0.0053, pooled +0.0066 Brier; defensive ordering 0.17-0.24 vs the table's 0.12-0.15). #139's recorded verdict is unchanged — this does not retro-pass S1 — but Route C's input is no longer blocked: the rating's clean-sheet channel provably carries information beyond last season's table (the >0.5 top-band cap caveat still stands) | — |
| `terminal_value_study` (+prereg, +note) | Phase 4's first test, and a clean negative recorded as declared: a GP over the squad state (remaining weeks, bank, sell value, transfers, chips in hand; 2,860 rows from 80 committed chains) loses to the constants-plus-average baseline **pooled (−22.19 MAE) and in all four held-out seasons**. The mechanism is legible: the target is a sum of weekly nets so the baseline owns the length effect; season-level shift dominates the error and the GP extrapolates worse than a wrong constant; the fitted kernel flattens every feature beyond remaining-weeks (length-scales 10–30 sd) while bending to 0.44 sd on remaining-weeks itself. **The planner keeps the holding-value constants.** Two future representations recorded for a *new* prereg, not a retry: per-week-normalised target, and features describing the squad rather than the wallet | — |
| `anchored_differential` (prereg; measured, fails localized) | The successor rival_calibration's failure names, gate fixed the same day and before any code: candidate-vs-crowd decomposed through the risk-neutral anchor - (candidate - risk_neutral) under scenario draws + (risk_neutral - crowd) resampled from the measured edge series - so the scenario-implied gap to the crowd (the proven fiction) never enters. Declared: same population, three deterministic candidate families spanning the shared-player spectrum (risk-neutral / contrarian / shadow), degenerate-identity clause within 0.02, calibration within 0.10 per horizon AND per family, ordering sanity, opt-in with bit-for-bit fallback. Two failed constructions in a row would itself be publishable evidence | — |
| `overlap_scaled_edge` (prereg; measured, fails - line closed) | The third construction, gate fixed the day the second failed: the crowd's edge belongs to its players, so each squad faces the resampled edge scaled by how little of the crowd it holds (captain-weighted starter share, declared once, not a knob); the degenerate identity becomes structural (the scaling term is identically zero for the anchor). Gate: all nine family-horizon cells within 0.10 with intervals containing zero, the two already-calibrated families protected within 0.03 cell-for-cell, opt-in with bit-for-bit fallback. A third failure is itself publishable: windowed crowd-relative probabilities would then be declared not claimable from this scenario model | — |
| `overlap_calibration` | The third construction, measured as declared - and the line's own stop-rule now binds. Identity structural (0.0000), the two protected families byte-identical to anchored (clause passed), but shadow moved the WRONG way (h3: -0.149 -> -0.171): a projection-suboptimal candidate wins against the crowd mainly when the crowd's edge draws negative, and scaling the edge shrinks that lifeline too - the mechanism cuts both tails. Three failed constructions as pre-declared: **windowed crowd-relative probabilities are not honestly claimable from this scenario model**, published as the program result; the product ships price tags, and the chase stops until the scenario model itself changes (player-level, ownership-informed projection - the prediction side's future line) | — |
| `anchored_calibration` | The anchored construction, measured as declared - and the trajectory converges: the risk-neutral family is calibrated at every horizon (gaps -0.013/-0.021/-0.047, identity exact 0.0000) and the contrarian family nearly perfectly (-0.003/-0.017/-0.010), against the direct construction's +0.42..+0.61. The one failure is localized and single-directioned: the shadow family (crowd's top-eight held) UNDER-claims by 0.09-0.15 - because the crowd's edge belongs to its players, not to the squad as a unit, and a candidate holding eight of them inherits a share the decomposition wrongly assigns wholly to the anchor-vs-crowd term. Verdict fails per the declared per-family gate; the successor mechanism (overlap-scaled edge) gets its own prereg | — |
| `rival_calibration` | The amended prereg's clauses 3-4, run and **failed as declared**: over 393 cells (139/131/123 per horizon, ~28x the first windowed run's population) the resampled edge deflates claims in the right direction (h=1: 0.841 -> 0.763 against the constant) but calibration is not close - realized ahead-share is ~0.29-0.35 at every horizon while claims stay 0.76-0.90, gaps +0.42/+0.55/+0.61 with 90% intervals nowhere near zero. The diagnosis the numbers force: the fixed squad *is* the risk-neutral squad and the edge series *is* template-minus-risk-neutral realized, so reality's differential distribution is the edge distribution itself - the scenario side's projection gap and noise are a fiction for this pair. Probabilities stay off the site; the next attempt (anchored differential: candidate-vs-crowd = (candidate - risk-neutral)_scenario + (risk-neutral - crowd)_resampled) needs its own pre-registration | — |
| `template_rival_strength_2021-22` + `template_rival_strength_2022-23` + `template_rival_strength_2023-24` | The crowd's weekly edge series for the other three development seasons, measured for the amended rival_scenario prereg's leave-one-season-out resampling: means +3.49 / +5.89 / +6.62 (2024-25 was +7.19 - the crowd is ahead in every season), weekly sd 17.3 / 20.1 / 20.3 (remarkably stable), lag-1 autocorrelation -0.07 / -0.30 / -0.29 (no persistence anywhere). The empirical series are the sampling pools; nothing is fitted | — |
| `rival_scenario` (prereg, amended 2026-08-23 pre-run) | The gate a probability claim must pass before the site may print one, fixed before any implementation: the rival's eleven scored through the **same scenario draws** as ours (shared players provably cancel in every scenario; covariance priced, not assumed), constant edge kept as the location term. Declared: 147 folds, h=1/3/5, pooled `\|claimed - realized\| <= 0.10` with a 90% interval containing zero, h=1 not sacrificed, and mode-safety ordering stable - a reordering is a finding to publish, not a knob. Failing keeps price-tags-only, passing lets P(ahead) join them with the calibration artifact linked beside it. Amended before any run: the rival's player draws were already shared (code read caught the misdescription); the missing variance is the *edge's own* - weekly sd 18.0, lag-1 autocorrelation 0.07 - so the mechanism is a per-week edge draw resampled leave-one-season-out from measured series, and the population moves to the cheap fixed-decision path (~140 windows/horizon vs 5). Gate unchanged | — |
| `rival_edge` (prereg + re-runs) | The crowd's measured +7.19/week enters the scenarios as a rival-side constant (`rival_edge_points`, default 0, zero-edge identity proven bit-for-bit; visible in every diagnostics block; threaded through the rank solver, fixed-decision comparison, goal menu and plan selector). Gate passed as declared: windowed claims deflate toward reality (h=3: 0.97->0.87, h=5: 0.99->0.89, h=1 gap narrows 0.29->0.17) -- and no more than that: 0.87 against a realized 0.00 is still fiction, so crowd-relative window claims remain diagnostics, not probabilities. What the edge unlocked: the competitive modes **separate on the plan menu for their stated reasons** -- Garantici/Agresif take the single-chip `bboost_gw21` (P=1.00, 3xc 0.97-0.98, no_chips 0.92), Asiri Agresif pays for both chips because only the biggest swing clears +5 against an edged crowd (1.00 vs 0.98) | — |
| `mode_plan_selection` | The product flow run once end to end: one window (2024-25 GW20, horizon 3), a held squad, a menu of eight candidate plans (no chips, the planner's choice, every forced BB/TC placement -- `ChipAvailability.forced` already existed), all priced per scenario on the same joint paths (TC x3, BB bench, hits charged), four play modes picking from one menu, **78 seconds wall clock** for the whole flow (its own artifact's number, `mode_plan_selection.md:4`), inside the 5-minute-per-decision budget. Saf Puan picked `planner_choice` (341.6 expected); the three competitive modes all picked `3xc_gw20` because the under-priced crowd made every chip plan P~1.00 and the declared tie-break (fewer chips) decided -- the selector's arithmetic is right and its inputs cannot let the competitive modes separate until the crowd's edge enters the scenarios (see `windowed_rank_note`) | — |
| `windowed_rank` (+note) | First end-to-end window claims: joint paths -> `as_window_scenario_set` -> the unchanged rank objective -> P(ahead of the crowd) at horizons 1/3/5 over five 2024-25 origins. The machinery works (horizon-one fingerprint-equal by test) and the claims against the crowd inflate exactly as `template_rival_strength` predicts: claimed 0.89/0.97/0.99 vs realized 0.60/0.00/0.00 -- the crowd scores ~+7.19/week above the projection the scenarios are centred on, and over 3-5 weeks that compounds past any squad edge. Rule bought: a rival-aware probability is only as honest as the rival's representation in the scenarios; the crowd's measured edge must enter as a rival-side location term before windowed claims are shown as probabilities | — |
| `template_rival_strength` | The rival the competitive modes will actually face, measured before pricing anything against it: the ownership template (the crowd's most-owned legal eleven, rebuilt weekly, captained by its most-owned starter) beats the control's risk-neutral squad by **+7.19 realized points a week** [+2.35, +11.97] over 37 folds of 2024-25, finishing ahead **70%** of weeks, sharing only 3.4 starters and agreeing on the captain 38% of the time. The crowd carries information the control does not — team news, form, fixtures priced by humans — so 'beating the crowd' is a much higher bar than beating the synthetic rival every rank measurement used so far | — |
| `mode_rehearsal_garantici` + `mode_rehearsal_asiri_agresif` | The two per-mode rehearsals behind the mode_price_list row, 2026-08-19, 37 folds of 2024-25 against the synthetic risk-neutral rival on a budget sweep: Garantici at budget 0 claims P(ahead) 0.72 vs realized 0.22 (the in-sample optimism the held-out claim machinery later addressed), changes 1.35 starters and pays a realized 1.65; Asiri Agresif claims 0.17 for its ahead-by-more-than-five event vs realized-ahead 0.38, changes 2.73 starters, captain moved in 30% of folds. Nothing promoted; the price-list gate consumed these as inputs | — |
| `mode_price_list` (+prereg, +two mode rehearsals) | The play-mode selector's evidence, gated by a pre-registration written before any number existed — **and the gate passes**: Garantici (margin −0.001) cuts P(behind) from Agresif's 0.46 to **0.27** at budget 0, fold-paired difference **−0.189 [−0.297, −0.081]**; at budget ≥2 it copies the rival outright (P(behind)=0, cost 0) — the correct solution once a level finish counts. Each mode is best at what its name claims (pooled P(behind) lowest under Garantici; P(ahead>5) highest under Aşırı Agresif) and every held-out claim sits within ten points of the frequency of the event it claims. One evaluator defect found and fixed in the process: Aşırı Agresif's claim was first compared against strictly-ahead instead of ahead-by-more-than-five, failing honesty spuriously (0.17 vs 0.38); against its own event the gap is 0.02. Prices are against the synthetic risk-neutral rival; re-pricing against the crowd waits on the rival-side location term (`windowed_rank_note`) | — |
| `rank_cost_calibration` (+note) | Baseline run before a scheduled fix, and the pre-registered gate cancelled the fix: the rank objective's cost side is **not** measurably miscalibrated — pooled expected **+1.339** vs realized **+1.365**, gap **+0.026 [−1.022, +1.091]**, no budget's interval excluding zero and the sign not even consistent. The remembered '+1 expected / +3 realized' does not reproduce. The claim side is honest (claimed 0.35–0.44 vs realized 0.35–0.41 against in-sample 0.52–0.60, so `held_out_half` works). What the run found instead is larger: **the objective finishes behind more often than ahead at every budget** (behind 0.41–0.51 vs ahead 0.35–0.41) against a rival inside its own feasible set — correct arithmetic for *strictly ahead*, where a tie is worth zero, and a reason to argue about the target rather than the accounting. Caveat: only budget 0 is mostly solver-proven (30/37) | — |
| `scenario_path_dependence` | The first thing the new multi-week paths were pointed at, and it corrected the claim they were built on. Over 2024-25 GW20-22, 711 players, 128 control folds, same projection both arms: a path is **0.983x** as wide as three independent weeks, not wider — eleven-level p05-p95 **69.59 vs 72.32**. A player who beats his projection one week tends to fall back the next, and that mean reversion cancels part of what independent draws add up. Small, and the opposite sign to the intuition the module was written with; independence still asserts a dependence structure the data does not have | — |
| `preseason_fixture_difficulty` (+prereg) | Evidence pinned before it could expire: the live capture `fpl-live-20260816T081259Z`, taken **130.8 h before the first 2026-27 kickoff**, records all 380 fixtures' published difficulty with payload checksums. Also records what was *not* published — `strength_attack_*`/`strength_defence_*` are **zero for all 20 clubs** pre-season while a finished season's archive carries them on a thousand-point scale, so the archive's strength columns are demonstrably not pre-season values. The difficulty column's admissibility is now a declared measurement: comparison, hypotheses and a 5% threshold fixed in `preseason_difficulty_prereg.md` before the season started | — |
| `opponent_projection_study` (+note) | Stage three, 110 folds of the operational control: the fitted Dixon-Coles adjustment **fails** — error +0.0016 [+0.0011, +0.0021] but ordering −0.0003 and **−0.91 realized points per fold**, because its MID/FWD slope fits **negative** (−0.041): the control's rolling form already carries the club's scoring, so the rating double-counts it. The published-difficulty adjustment **passes** all three clauses (+0.0121 error, +1.74 points per fold [+0.52, +3.03], positive every season ≈ +64/season) **but is not carried forward**: an admissibility check shows the archived rating tracks its own season better than the previous one in 2023-24 and 2024-25 (0.940 vs 0.372), i.e. it was not knowable at the deadlines it is used at — a data-contract question for `features/fixtures.py`, raised to the data side | — |
| `opponent_signal` | The Dixon-Coles rating refit once per gameweek, recorded as the attacking and defensive signal it implies per club. **3,604 of 3,620 club-gameweeks carry a signal; all 16 gaps are 2020-21's opening gameweek**, where no rating can exist and where the shifted per-90 feature is missing anyway. An input a declared candidate would read; decides nothing on its own | — |
| `route_a_declaration` | **Route A frozen before its run** (#88). One component changes — `expected_points_rate` gains the player's own club's Dixon–Coles attacking and defensive signals. Both pooled coefficients declared **positive a priori**, with the reasoning: two separated channels should not cancel the way S2's single conflated difficulty integer did. Gate is on the pooled coefficient; per-position slopes are diagnostics. Input bound by frame digest `8d977552…`, re-derived in the run. **No shared contract constant moves** — Route A carries `route_a_opponent_signal_rate_v1`, so #43's freeze stays valid (verified: its fingerprints are unmoved). Written after the fw10 holdout, against an unchanged control | #88 |
| `capture_season_phase` | What a capture's cumulative element counters describe. **A pre-reset capture echoes the completed season exactly** — 454 of 461 players identical to 2025-26 totals — and a capture taken after the season's first kick-off agrees with only 55, with 404 players' accumulated minutes *falling*, which a counter that only accumulates cannot do. The reset instant itself sits in the 90 minutes between the opening deadline and the first kick-off and is **unobserved**, so that window refuses rather than guesses | — |
| `in_season_blend_benchmark` | The GW2 projection walked forward over the 147 development folds against what it stands in for. Reading the season so far is worth **+13.78 [11.68, 16.86] realized squad points, 4/4 seasons**, against ignoring it, and both controls reproduce to the digit. **The weight axis does not resolve**: the differences it asks about are 0.16–0.84 while re-running one configuration moves it up to 0.22, because the tie-break that picks between equal-objective squads does not finish on every fold (#192). Per-configuration solve health is in the record; absolute levels are optimistic because the opening price prior is in-sample for these seasons | — |
| `opening_prior_exposure` + `opening_prior_exposure_correction` | The `in_season_blend_benchmark` caveat's own named follow-up, over its 147 folds and the same 101,447 rows. **The differing reliance is real and large**: the archive-fed control takes **0.79%** of its projected points from `FITTED_OPENING_PRICE_COEFFICIENT`, the blend **29.58%** and the carry-over floor **35.26%**, with 45.59% of the latter two's rows touching it against 0.71% of the control's. **And it is near-absent where a decision is made** -- 0.00% / 0.46% / 0.00% of a squad-shaped selection -- because the reliance sits on players the prior prices at a point or two, which is who an optimizer does not pick; the two facts only mean anything read together. Refit walk-forward the constant comes out **higher** every season (+0.0352 on one season of history, +0.0038 on four), so if the in-sample fit biases levels it is downward, the opposite of what the caveat feared, and the level shifts are at most +0.0447 mean projected points. The line the record exists for: **the blend projects −0.1696 against the floor while scoring +13.78 realized squad points above it**, so no number here corrects the headline -- projection and decision disagree in *sign* on that very pair, and the decision-level re-score is named as the next step rather than estimated. Reliance is measured as `c·d(points)/dc` by re-projecting at a perturbed coefficient rather than by re-deriving the precedence rules, so it cannot drift from them; two probe scales agree to 2e-10. The first run loaded 2025-26 before filtering and is non-binding; the corrective note fixes the load boundary without changing the population or estimator | — |
| `in_season_residual_export` | The out-of-sample residual history for `in-season-carry-over-v1`, the model that decides from gameweek two. **101,447 rows over 147 folds**; residual mean −0.3783, sd 2.2171. The control export already recorded carries `form_window_05_v1`, a different model, and calibrating one model's spread on another's is what #45 exists to prevent. Opening gameweeks are refused rather than excluded quietly: this history cannot describe a carry-over-and-price-prior projection. An input, not a calibration claim | — |
| `residual_signal_scan` | Enrichment fields the archive holds but no model reads, strictly lagged against the control residuals (101,447 rows): **recent luck** (returns − xGI, last 6) is monotone with a **1.07-point** residual spread — hot players are over-predicted by 0.74; **recently moved** players (594 rows) are under-predicted by **+0.62** with the model *widening* the raw gap (ratio 2.14); the source's own `xP` adds +0.48 in its top quartile; xGI/90 itself 0.32 (non-monotone); ownership ~nothing (0.10) | — |
| `shadow_calibration_in_season` (+`phase2_shadow_calibration_prereg`; measured, **gate P1 passes, verdict abstained**) | The first calibration evidence for `in-season-carry-over-v1` — the model that actually decides live from gameweek two, and the one model no committed calibration wrapped: every fitted calibration on record wraps the archive-fed control `form_window_05_v1` instead, which is the mismatch #45 exists to prevent. Pre-registered in `phase2_shadow_calibration_prereg.md` before the runner existed; bound to one export by fingerprint (`in_season_carry_over_blend`, sha256 `7543a949…`, 101,447 rows / 147 folds, preflight PASSED 24 checks), fit on 2021-22..2023-24 and scored frozen on 2024-25's **37 folds / 26,303 player-gameweek rows**; h=1 only; 2.7 s. **Gate P1 (player coverage) passes at every level**: pooled 0.9062 (|Δ| 0.0062 ≤ 0.03), single 0.9030 over 25,939 rows, double_plus 0.8736 over 364 rows (both |Δ| ≤ 0.05); mean interval width 6.05 pooled, 10.56 on doubles, reported not gated. **The verdict is `abstained`, not a pass**: the pre-registration names three gates and this player-level runner asked one, so `S1_squad_pit_location` and `S2_squad_lower_tail` went unasked and `calibrated_internal` is not claimable — a partial protocol is not a verdict. Nothing is promoted, no member-facing surface, contract or evidence status changes, and no probability is published on any branch of this result. Recorded caveats: the double-gameweek cell is 364 rows, thin enough that its pass is weak evidence; the export declares no `predicted_points_decimals`, so its digest is byte-stable on this machine (two generations agreed) but not proven cross-platform; and the run is one measurement, not a re-run distribution | — |
| `opponent_strength_signal` | Control residuals still move with opponent strength: **+0.162** attacking (monotone), **+0.322** defensive; effect is *larger* after the model than before it (1.24x / 1.06x) — unspent signal | #87 |
| `strategy_bench` (+`strategy_bench_prereg`; measured, **gate 1 fails at every horizon**) | The bench pre-registered in #279 before the catalogue existed, applied to the declared four development seasons (147 folds, h=1/3/5, template rival, bands overlap ≥9 / ≤5 / unconstrained; first run 3797 s, corrective run 8619 s). **Gate 1 (separation) fails at all three horizons**: the high-overlap band does not fall behind its rival less often than the differential band — h=1 mean −0.027 [−0.075, +0.027], h=3 **+0.046** [0.000, +0.100] (the wrong sign), h=5 +0.012 [−0.035, +0.070]. **Gate 2 (direction) passes at h=1 and h=3 only as a tie** — at h=1 all three bands win big in exactly 36/147 folds — and **fails at h=5** (differential 0.228 below control 0.237); the implementation averages each band over its *own* feasible folds, so at h=3/h=5 this is not the common population the prereg declares. **Gate 3 (price honesty)** passes both bands at h=1 and h=3 (the floor-9 tag *over*states: realized cost runs 4.8–5.3 points below claimed) and **fails for the differential band at h=5** ([+0.028, +0.172] entirely above zero). **Gate 4 (h=1 not sacrificed) passes**: 147/147 folds re-solve to an identical squad, XI and captain — as implemented this is a determinism check between two solves, not a comparison against the shipped publication. **Nothing earns `gated_pass`**: `ortak-koru` and `fark-yarat` stay `prereg_open` and may not be named safer or riskier at any horizon. Diagnostics: the ceiling-5 band is non-binding in 94–95% of folds (the control's own overlap is already ~3.5), the floor-9 band binds in 97–98% but is infeasible in 10/140 folds at h=3 and 28/114 at h=5; the control itself did not prove optimality in 7 folds at h=3 and 33 at h=5, and those folds are dropped rather than imputed. **Protocol note**: the first run (8f8d3cb) loaded the panel with the loader's default season list, which includes the locked 2025-26 holdout — a no-read violation, so that run is procedurally invalid as binding evidence and stands in history only; the corrective rerun under the prereg's post-run amendment (season validation before any loader call, explicit 2020-21..2024-25 history list, `holdout_untouched` derived from the loaded panel) reproduced every analytical number bit for bit, which also confirms the holdout rows never influenced a result | — |

## Season record

**Not measurements.** These two are the live season's own record, appended on settle day from
the frozen ledger entry, and they are the only rows here that describe the operational control
scoring rather than an experiment evaluating one. They belong in this index because rule 1
covers every committed artifact, not only experimental ones — and because the
projection-versus-reality series is what a reader is most likely to come looking for. The
header's claim still holds: recording what the control scored promoted nothing and changed
nothing.

| Artifact | Finding | PR |
| --- | --- | --- |
| `weekly_scorecard` (record) | Projection versus reality, one row per settled gameweek. **GW1 (`opening-carry-over-v1`): projected 56.08, realized XI 26, net 26, error −30.08.** The captain returned 4 points doubled — a 15% share of the realized XI, against a projection that had him as its largest single line. First entry of the series, so it establishes the sign convention (realized minus projected; negative means the projection was optimistic) and nothing about variance yet | #258 |
| `season_ledger_2026-27` (record) | The `season_ledger_v1` summary of every live decision: GW1 from capture `fpl-live-20260821T143619Z-11bc603a8e1c`, solver `OPTIMAL`, 0 transfers, 0 hits, no chip, **96 players excluded as unavailable**. Settled gameweeks 1; mean realized 26.0; mean projection error −30.1. Raw entries stay local under `data/ledger/` with per-file checksums, so this file is the committed half of a record whose evidence is deliberately not committed | #258 |
| `shadow_calibration_in_season_corrected` (corrective binding run; supersedes the non-binding `shadow_calibration_in_season` artifact) | Corrective execution under the pre-committed serialization/create-once amendment. The deciding model's 101,447-row residual export is serialized to exactly nine decimals and bound by sha256 `17f88e6e…`; manifest preflight passes 24/24. The immutable report records start/end UTC, 3.45 s elapsed, seed 0, an explicit empty warnings list, and `working_tree_dirty: false`. Gate P1 reproduces the earlier numbers exactly (pooled 0.9062; single 0.9030; double-plus 0.8736), but the verdict remains **`abstained`** because S1/S2 were not run. The earlier `7543a949…` artifact is retained only as a non-binding audit trail; it cannot unlock any status | — |
| `shadow_calibration_squad` (full-protocol binding run; the first squad-level calibration of the deciding model) | The whole pre-registered protocol, measured once under the four squad-gate amendments: P1 merged from the `shadow_calibration_in_season_corrected` artifact by digest `9dd3ec75…`, S1 and S2 measured here on the same export (`17f88e6e…`, 101,447 rows), fit on 2021-22..2023-24 and scored frozen on 2024-25's 37 folds; 200 scenarios at seed 11; 612 s; seed 11; empty warnings list; `working_tree_dirty: false`; panel seasons loaded exactly 2020-21..2024-25. The frozen selection-optimism shift is **-7.4307** points, fitted over **102** development folds (2021-22-gw10 to 2023-24-gw38) after the declared eight-fold burn-in. **P1 passes** (pooled 0.9062, single 0.9030, double-plus 0.8736) and **S1 passes** — mean PIT **0.4922**, inside [0.43, 0.57], so the shifted squad distribution is centred where the protocol asked. **S2 fails**: the realized squad score falls below its own scenario tenth percentile in **8 of 37** folds, a rate of **0.2162** against the pre-registered [0.04, 0.16]. The squad distribution's lower tail is too thin — it understates downside risk by roughly a factor of two — and this is the recorded result. **The verdict is `failed`**, which is a valid outcome and not a reason to retune: no threshold, seed, shift, scale or fit population moved after it, and none may without a new pre-registration. Nothing is promoted, no member-facing surface, contract or evidence status changes, and no probability is published anywhere. The fold-level bootstrap is diagnostic only and decided nothing: mean PIT 90% [0.4017, 0.5838], tail rate 90% [0.1081, 0.3243]. Recorded caveats: the 37 evaluation readings share one scenario draw (one frozen history at one seed), so that interval understates the uncertainty; at 37 folds S2 can only land on multiples of 1/37, so the gate is really the test that the count is 2 to 5; and the double-gameweek P1 cell is 364 rows, thin enough that its pass is weak evidence | — |
| `phase2_tail_diagnostic` (+`phase2_tail_diagnostic_prereg`; development diagnostic, **promotes nothing**) | Why the squad distribution's lower tail is thin, asked on development data after `shadow_calibration_squad` recorded a **failed** verdict that this study does not touch. One factor is swept — `dispersion_scale` at the four levels 1.00/1.15/1.30/1.45, committed before any of them was measured — over 139 folds; each fold is optimized once and its scenarios generated once, so the arms share their draws by construction, and the frozen shift of -7.4307 is applied unchanged to every arm. The control arm reproduced the recorded S1 and S2 exactly (0.4921621621621622 and 0.21621621621621623, tolerance 1e-12), which is what made the comparison admissible. **Classification `scale_sufficient_candidate`, eligible set {1.45}**, decided on 2023-24 validation alone: there 1.45 gives mean PIT 0.4476 and 3/37 below-q10 (0.0811), both inside the existing bands, while 1.00 gives 0.4235 and 11/37 (0.2973) and fails both. **No winner is chosen and nothing is promoted** — selecting from the eligible set needs its own pre-registration. The screening seasons qualify the reading rather than confirming it: at 1.45 the tail rate is 2/29 (0.0690) in 2021-22 but **0/36 in 2022-23**, below the 0.04 floor, so one global scale trades one season's tail against another's and no declared level satisfies both gates in all four seasons. 2024-25 is labelled development sensitivity, seen before this study was written and unable to change the classification; it shows 1.15/1.30/1.45 all inside both bands. Descriptive only, deciding nothing: the common gameweek shock co-moves weakly with the squad gap (Pearson 0.382, sign agreement 0.547) and barely separates the below-q10 folds (mean common component -0.431 against -0.360 elsewhere), while the **captain's realized error separates them sharply** — -5.93 on below-q10 folds against +0.15 elsewhere, worst -8.66, at a scoring policy that counts the captain twice. The fold-level bootstrap is diagnostic only and decided nothing (validation at 1.45: mean PIT 90% [0.3715, 0.5261], rate 90% [0.0270, 0.1622]). Locked 2025-26 holdout not read; panel seasons loaded 2020-21..2024-25; no member-facing surface, contract or evidence status changes and no probability is published | — |
| `phase2_captain_attribution` (+`phase2_captain_tail_attribution_prereg`; **development reuse, exploratory — not a promotion gate and not independent confirmation**) | Whether the squad S2 failure is carried by the extra captain copy the scoring policy adds. Same 139 folds, same decisions, same scenarios and seed as `phase2_tail_diagnostic`; one decision and one scenario matrix per fold read four ways, with **only** the captain's second copy removed — his own scenario column on one side, his realized points on the other — while he stays a starter with his ordinary points and nothing is reoptimized. The full arm reproduced the recorded `shadow_calibration_squad` numbers exactly (mean PIT 0.4921622, 8/37 = 0.2162162), which the runner requires before it will write. **Classification `shared_tail_failure`** on the frozen 2024-25 season: removing the captain bonus moves the tail only from **8/37 (0.2162) to 7/37 (0.1892)**, still above the [0.04, 0.16] band, and the same holds under the unshifted companion convention (10/37 to 13/37), so the reading does not depend on which location convention was used and the declared veto did not fire. The other seasons qualify that reading rather than repeating it, and one of them moves a verdict: 2023-24 fails S2 with the captain bonus and still fails without it (11/37 → 9/37, 0.2973 → 0.2432, above the band both ways), 2022-23 has no S2 failure under either arm (2/36 → 3/36), and **2021-22 is the one season whose S2 verdict changes** — 6/29 (0.2069, above the band) becomes 4/29 (0.1379, inside it). That single flip is convention-dependent and trades one gate for the other, so it is not a repaired season: the unshifted companion stays at 7/29 (0.2414) with the bonus and without it, and the same removal pushes 2021-22's S1 out of its band (mean PIT 0.5233 → 0.5726 against [0.43, 0.57]). The captain component is therefore real and season-dependent, and still not the general explanation: in both seasons that fail S2 under both conventions — 2023-24 and 2024-25 — removing the extra copy leaves the failure standing. **On the classification population the rest of the squad carries the same calibration problem**, so a captain-specific correction is not the next step. Descriptive only, deciding nothing: over 139 folds the captain's second copy promised 7.94 points and returned 6.87 (mean error −1.07, negative in 94 folds), and on the 27 full-score tail-failure folds its error averaged **−6.13** against **+0.15** elsewhere — a real concentration that nonetheless does not carry the failure. The correlation with the full-score error is 0.649 but is inflated by construction (that error contains the captain twice); against the captain-bonus-removed error it is 0.395. The decomposition identity was checked against the canonical scenario matrix and each fold's realized frame and held on all 139 folds. Nothing is promoted, `promotion_eligible: false`, 2024-25 was seen before this study was written, the locked 2025-26 holdout was not read, and no probability is published | — |
| `phase2_downside_dependence` (+`phase2_downside_dependence_prereg`; **development reuse, exploratory — not a promotion gate and not independent confirmation**) | Whether the canonical scenarios understate several members of the fixed selected XI falling below their own scenario-marginal q25 together. Same 139 folds, decisions, scenarios and seed; the captain appears once and the bench does not enter. The full-score control reproduces the recorded PIT and S2 exactly before the study writes. **Classification `marginal_and_joint_downside_miss` on 2023-24**: realized starter downside is 0.4816 against scenario-expected 0.2500, a paired gap of **+0.2316 [0.1800, 0.2783]**; realized all-pair co-downside is 0.2423 against 0.0636, gap **+0.1786 [0.1341, 0.2230]**. The marginal miss is present in every season and grows across the declared sequence (+0.1074, +0.1919, +0.2316, +0.2807); on the 27 recorded full-score below-q10 folds it reaches +0.3999 and the realized downside count exceeds its scenario q90 in 25. **This does not yet prove missing correlation**: for eleven players the all-pair rate is mechanically `choose(K,2)/choose(11,2)`, so an inflated downside count `K` necessarily inflates the pair statistic. The pre-registered mixed classification is therefore the result, not a euphemism — marginal location/shape must be separated before a dependence claim. Same-team and different-team gaps are both positive in validation (+0.2046 and +0.1777), which rules out a simple "only teammates" reading but remains descriptive under the same marginal confounding. The canonical scenario covariance contribution is not uniformly too small (positive in 2021-22..2023-24, negative in 2024-25), so no shock or copula is selected. One binding execution, 1,246 s, empty warnings, source digests recorded, panel limited to 2020-21..2024-25, locked 2025-26 not read, nothing promoted or published. The next question is why selected starters cross their own marginal downside threshold too often — appearance/minutes failure, removed player-location bias, or residual tail shape — before any joint-dependence implementation | — |
| `phase2_marginal_downside_sources` (+`phase2_marginal_downside_sources_prereg`; **development reuse, exploratory — not a promotion gate and not independent confirmation**) | Accounting for the marginal miss before changing the generator. Same 139 fixed-XI folds and canonical scenarios; 1,529 starter rows; full-score replay exact; one binding execution (1,444 s), empty warnings, source digests recorded, panel limited to 2020-21..2024-25 and locked holdout not read. **Classification `full_appearance_performance_dominant` on 2023-24**: of 94.25 excess starter downside events, `minutes == 0` contributes 29.25 (31.0%), `1-59` contributes 9.75 (10.3%), and **`60+` contributes 55.25 (58.6%)**. The result strengthens in 2024-25 (82.50 of 114.25, 72.2%) and is already present in 2022-23 (49.26 of 76.01, 64.8%); 2021-22 differs, with low minutes carrying 99.2% and the 60+ contribution indistinguishable from zero. Pooled, 60+ appearances are 86.8% of rows and carry 58.8% of the excess. This is not a simple mean-location error: the 60+ mean residual is near zero pooled (-0.044; -0.182 validation, -0.240 sensitivity) while its realized q25-downside rate rises from 0.2509 to 0.4804 across seasons, consistent with a miss in conditional marginal shape rather than only the mean. The omitted pre-fold player residual mean points the other way — positive in every season (+0.48 to +0.99) and correlated about zero with the later residual (-0.022 on validation) — so turning on player-location shrinkage is not selected and would risk worsening optimism. **Limits:** minute buckets condition on realized post-outcome minutes and are accounting, not causal identification; the blanket preregistration sentence asked for fold-bootstrap diagnostics, but the committed artifact supplies them for bucket excess contributions and only point summaries for omitted location, so the location reading is deliberately weaker than the classification. No appearance model, tail transform, shock, copula or production change is selected. The next preregistered question should distinguish whether hierarchical decomposition/recombination erases the selected-player left tail or whether the residual pool itself lacks the required projection-conditional shape | — |

| `phase2_conditional_marginal_shape` (+`phase2_conditional_marginal_shape_prereg`; **development reuse, exploratory — unresolved negative result**) | Whether direct empirical residual marginals repair the 60+ minute starter miss. Same 139 folds and 1,327 completed-appearance starter rows; the canonical replay and every season's prior 60+ bucket reconcile before writing; one binding execution (1,394 s), empty warnings, source digests recorded, panel limited to 2020-21..2024-25 and locked holdout not read. **Classification `conditional_marginal_shape_unresolved` on 2023-24**: canonical gap **+0.1608 [0.1093, 0.2107]**; direct raw-unconditional **worsens** it to **+0.2163 [0.1675, 0.2641]**; direct raw completed-appearance worsens it again to **+0.2422 [0.1913, 0.2947]**. This is not a fallback artefact — all 347 validation rows use their own player's raw pool in both direct arms. The thresholds explain the direction: canonical q25 is -2.674, raw unconditional -2.349, and completed-appearance -1.952, so decomposition/recombination actually supplies a more conservative left tail than either direct pool while still not enough. Removing historical low-minute outcomes makes the conditional q25 less negative and therefore cannot repair the observed 60+ miss by itself. The same ordering holds in 2022-23 and 2024-25; only 2021-22's canonical interval contains zero. Read with the preceding artifact: 60+ mean residual stays near zero while low-tail frequency rises across seasons, historical player location is positive and uncorrelated with the next residual, and direct player history does not calibrate the tail. **Therefore neither missing player location, source fallback, appearance conditioning alone nor marginal damage from hierarchical recombination is supported as the root cause.** The remaining candidate is a non-stationary or projection-conditional discrete return shape: optimizer-selected players near five expected points frequently return ordinary appearance points, with rarer hauls preserving the mean. This study does not prove that final mechanism; it narrows the next preregistration to expected-points/return-state conditioning and promotes nothing | — |
| `phase2_projection_conditional_returns` (+`phase2_projection_conditional_returns_prereg`; **development reuse, exploratory — inconclusive and no candidate opened**) | The pre-registered follow-up to the discrete-return hypothesis, over the same 139 fixed-XI folds and 1,327 completed-appearance target rows. It compares four-state return probabilities from position alone, position plus five fixed expected-points bands, and the same conditional rule over the last eight allowed folds; rows are scored with fold-weighted multiclass Brier loss, while q25 tail calibration is a non-regression veto. One binding execution (449 s), no warnings, full-score replay and prior completed-appearance populations reconciled, panel limited to 2020-21..2024-25 and locked holdout not read. **Classification `diagnostic_inconclusive` on 2023-24** because the recent conditional arm reaches its exact position-band cell for only **29.1%** of 347 rows, below the pre-registered 80% support floor (the full conditional arm reaches 99.7%). The measured directions do not justify relaxing that floor: full conditioning lowers Brier from 0.6908 to 0.6722, but its paired delta **-0.0186 [-0.0507, +0.0140]** does not establish improvement; the recent arm reaches 0.6687 and its direct delta against position is **-0.0221 [-0.0449, -0.0001]**, but it is mostly a fallback mixture and therefore cannot receive the declared label. More importantly, the pre-registered projection-localization contrast points in the opposite direction: high-projection minus low-projection ordinary-return gap is **-0.1698 [-0.2517, -0.0883]**, not above zero. Both conditional arms improve the descriptive q25 absolute-gap diagnostic, but Brier evidence, direct-source support and the claimed mechanism do not align. **No expected-points or recency candidate is implemented, no alternate band/support rule is tried, and the result does not authorize a production change.** It narrows the remaining work away from the proposed “high projections miss ordinary returns” mechanism and toward a separately preregistered state/mixture diagnosis that does not reuse realized minutes as a live input | — |
| `phase2_component_attribution` (+`phase2_component_attribution_prereg`; **development reuse, exploratory — component not localized**) | Exact accounting of the same 139 fixed-XI folds into appearance, attacking, defensive, bonus and negative-event points. Fixture rows are scored before double-gameweek aggregation; the five-part identity reconstructs all **126,737** player-gameweek outcomes and the selected-XI identity closes to at most **3.91e-14**. The prior 139-fold / 1,529-starter population and 2024-25 control replay reconcile exactly; panel seasons are only 2020-21..2024-25 and the locked holdout was not read. **Classification `component_not_localized` on 2023-24.** The result is nevertheless directional: normalizing only attacking surprise moves validation tail failures from **11/37 to 1/37** and sensitivity from **8/37 to 4/37**; tail-fold attacking surprise is about **19 points more negative** than elsewhere in both seasons (validation -19.29 [-24.28, -14.37], sensitivity -19.03 [-23.50, -14.46]). It misses the frozen localization gate because 1/37 = 0.027 falls *below* S2's 0.04 lower bound — the counterfactual over-corrects by the gate's discrete reading — not because the contrast or sensitivity direction fails. Appearance, defence and bonus do not establish a positive paired reduction on validation; negative events show neither contrast nor reduction. The pre-registration forbids choosing the strongest point estimate or relaxing the band after seeing it, so **no component model is opened and no production/scenario/optimizer path changes**. The honest next decision is a new preregistration that distinguishes a useful attacking signal from an over-normalized historical-mean counterfactual; this artifact alone does not select its generative form | — |
| `phase2_attacking_structure` (+`phase2_attacking_structure_prereg`; **development reuse, exploratory — attacking mechanism not localized**) | Follow-up to the Phase 2H attacking signal, asking whether completed-appearance attacking blanks are explained by player-level frequency, common-week co-movement or same-team excess before any generative model is chosen. The same 139 fixed-XI folds and 1,529 starters are replayed; one decision per fold supplies component scoring, captain and pre-match team membership, and every fold's captain-weighted attacking surprise reproduces Phase 2H within 1e-9. The Phase 2H artifact is digest-pinned (`4023b06b…`), control replay is exact, execution is 464 s with no warnings, panel seasons are only 2020-21..2024-25 and the locked holdout was not read. **Classification `attacking_structure_not_localized` on 2023-24.** The marginal blank gap is equivalent under the frozen ±0.05 rule: **+0.0078 [-0.0325, +0.0493]**. With that prerequisite met, none of the dependence references establishes positive excess: blank-count dispersion `O` is **+0.225 [-0.376, +0.839]**, different-team centred co-movement `U` is **+0.0037 [-0.0047, +0.0123]**, and same-team incremental contrast `K` is **+0.0134 [-0.0161, +0.0427]**. The already-observed 2024-25 sensitivity points slightly negative for all four metrics (`G` -0.0122, `O` -0.0047, `U` -0.0026, `K` -0.0163), so it supplies no directional rescue. A positive same-team contrast appears in 2022-23 screening but does not repeat in validation or sensitivity and is not selected. Blank probabilities are finite-history, calendar-unadjusted player-gameweek references; 45+45 double-gameweek minutes can qualify as completed, and estimation error can contribute to joint metrics. The result therefore **does not localize** the downside failure to marginal bias, common-week excess or same-team excess under this fixed reference; intervals crossing zero are absence of established evidence, not proof that those mechanisms cannot exist. No alternate event threshold, fixture split, hierarchy, Monte Carlo change, candidate transform, promotion or production change is authorized | — |
| `phase2_attacking_hurdle` (+`phase2_attacking_hurdle_prereg`; **development reuse, exploratory — diagnostic inconclusive**) | Exact follow-up to the Phase 2H attacking signal, splitting each selected starter's captain-weighted attacking surprise into return occurrence and positive-return severity with `occurrence + severity = attacking surprise`. The same 139 fixed-XI folds and 1,529 starters replay Phase 2H and Phase 2I exactly; the maximum recorded eligible-fold identity error is 5.33e-15. The runner checked 1-4-positive rows fail-closed during execution, but the artifact does not serialize an identity maximum for those excluded rows. Source artifacts and the exact residual export are digest-pinned, requested and loaded panel seasons are 2020-21..2024-25, and the locked 2025-26 holdout was not read. **Classification `diagnostic_inconclusive`.** The prereg required every one of a fold's eleven starters to have at least five positive historical returns (or the declared zero-positive fallback) and at least 30 eligible folds in both validation and sensitivity. Finite histories leave only **5/37 eligible folds in 2023-24** and **10/37 in 2024-25**; 32 and 27 folds respectively contain at least one Phase 2H pool with only 1-4 positives. Within that small supported subset, occurrence estimates were: validation contrast -27.05 [-39.01, -14.77] with q10-failure reduction +0.40 [0.00, +0.80], and sensitivity contrast -19.02 [-26.98, -10.62] with reduction 0.00 [-0.20, +0.20]. Severity intervals include zero in both seasons. These descriptive estimates do not rank or localize the mechanisms: five and ten folds cannot carry the frozen gate, and the validation occurrence-reduction interval also touches zero. Therefore this run neither localizes the failure nor rejects either mechanism; missing support is not imputed as zero, the 1-4 threshold is not relaxed after seeing the result, and no hurdle/count model, simulation change, promotion or production change is authorized. One binding execution, 513 s, no retune or rerun | — |
| `phase2_attacking_support_recovery` (+`phase2_attacking_support_recovery_prereg`; **development reuse, exploratory — return-reference signal only**) | Pre-registered support recovery for the inconclusive Phase 2J hurdle, using one fixed fill-to-five rule rather than lowering the five-return threshold or sweeping a pseudo-count. The exact Phase 2J rule is retained for zero and five-plus histories; only 1-4-positive histories borrow the missing count from the first target-player-excluded position or pooled reference with at least five positive player-gameweeks. The same 139 folds, decisions, scenarios, 1,529 starters and Phase 2H/I/J readings replay before the new arm is accepted. Support recovers from **5/37 to 37/37 validation folds** and **10/37 to 37/37 sensitivity folds**: all 46 validation and 47 sensitivity 1-4 rows use a position reference, no pooled fallback is needed there, and the newly eligible fold counts are 32 and 27. **Classification `attacking_return_reference_signal`.** In 2023-24 validation the return-reference contrast is **-15.63 [-20.25, -10.60]** and its fixed-q10 failure reduction is **+0.2162 [+0.1081, +0.3243]**, so both frozen validation gates pass. The 2024-25 sensitivity direction does not reverse them: contrast **-14.71**, reduction **0.00**. Positive-return severity is also lower on validation tail folds (-3.66 [-6.93, -0.62]) but does not pass its reduction gate (+0.0270 [-0.0541, +0.1081]), so it is not selected. The first component is deliberately **return-reference**, not a player-specific occurrence probability: sparse rows combine their exact historical mean with a leakage-safe position-or-pooled historical reference, and the artifact exposes fill fractions, source-by-support bins and alignment gaps. This result only authorizes a new preregistration for a return-reference modelling family; it does not fit a hurdle model, alter scenarios, promote a candidate or publish a probability. One binding execution, 1,002 s, empty warnings, exact residual and source digests rechecked before publication, panel limited to 2020-21..2024-25, locked holdout not read, no retune or rerun | — |

## Process references

`handoff_acceptance_checklist.md` · `candidate_declaration_review.md` ·
`gw1_blocker_report_template.md` · `fw10_holdout_plan.md` · `opening_week_runbook.md` ·
`artifact_preflight_spec.md` · `projection_horizon_contract.md`
