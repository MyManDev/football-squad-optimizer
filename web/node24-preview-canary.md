# Node 24 deployment preview canary

This disposable, unmerged file exists only to make the post-merge preview workflow exercise the trusted Node 24 action upgrades from PR #242. The canary succeeds when Upload, Identity, and Smoke are green; the pull request is then closed without merging.
